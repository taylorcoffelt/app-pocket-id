---
name: Stale

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  workflows:
    uses: einschmidt/workflows/.github/workflows/stale.yaml@6e89b387b7c75b60847138578ce5140bdac2923a # v0.1.1
