---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "55 2 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  workflows:
    uses: einschmidt/workflows/.github/workflows/lock.yaml@6e89b387b7c75b60847138578ce5140bdac2923a # v0.1.1
